import { useEffect, useRef } from "react";
import styles from "./DeleteThreadDialog.module.css";
export function DeleteThreadDialog({ open, threadTitle, onCancel, onConfirm }) { const cancelRef = useRef(null); const confirmRef = useRef(null); useEffect(() => { if (open) cancelRef.current?.focus(); }, [open]); if (!open) return null; const keyDown = (event) => { if (event.key === "Escape") { event.preventDefault(); onCancel(); } if (event.key === "Tab") { event.preventDefault(); (event.shiftKey ? confirmRef : cancelRef).current?.focus(); } }; return <div className={styles.backdrop}><section className={styles.dialog} role="alertdialog" aria-modal="true" aria-labelledby="delete-title" aria-describedby="delete-detail" onKeyDown={keyDown}><h2 id="delete-title">Delete investigation?</h2><p id="delete-detail">“{threadTitle}” and its saved transcript will be permanently removed.</p><div><button ref={cancelRef} type="button" onClick={onCancel}>Keep thread</button><button ref={confirmRef} type="button" onClick={onConfirm}>Delete investigation</button></div></section></div>; }
