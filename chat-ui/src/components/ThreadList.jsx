import styles from "./ThreadList.module.css";
export function ThreadList({ threads, selectedThreadId, onSelect, onDelete }) { return <nav aria-label="Saved investigations"><ul className={styles.list}>{threads.map((thread) => <li key={thread.id} className={styles.item}><button type="button" className={styles.thread} aria-current={thread.id === selectedThreadId ? "page" : undefined} onClick={() => onSelect(thread.id)}>{thread.title || "Untitled investigation"}</button><button type="button" className={styles.delete} onClick={() => onDelete(thread)} aria-label={`Delete ${thread.title || "investigation"}`}>×</button></li>)}</ul></nav>; }
