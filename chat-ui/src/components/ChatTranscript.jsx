import styles from "./ChatTranscript.module.css";
export function ChatTranscript({ messages }) { return <section className={styles.transcript} aria-label="Investigation transcript">{messages.length === 0 ? <p className={styles.empty}>Ask a precise question about current network state.</p> : messages.map((message) => <article key={message.id} className={`${styles.message} ${styles[message.role]}`}><span className={styles.role}>{message.role === "user" ? "Operator" : "Observed result"}</span><p>{message.content}</p>{message.pending && <span className={styles.pending}>Validating…</span>}</article>)}</section>; }
